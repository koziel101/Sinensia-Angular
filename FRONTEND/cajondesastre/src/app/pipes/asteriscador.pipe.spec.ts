import { AsteriscadorPipe } from './asteriscador.pipe';

describe('AsteriscadorPipe', () => {
  it('create an instance', () => {
    const pipe = new AsteriscadorPipe();
    expect(pipe).toBeTruthy();
  });
});
